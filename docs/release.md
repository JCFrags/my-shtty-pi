# Release gate

Do not release this repository until an independent reviewer approves the exact tree,
licenses, provenance, package tests, and secret scan.

A first release should contain source only. Build output, binaries, and package archives
need a separate review. Tag only packages that pass their own checks.
