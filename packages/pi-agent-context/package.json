{
  "name": "pi-agent-context",
  "version": "0.2.0",
  "description": "Cache-boundary environment snapshots and context observability for Pi.",
  "type": "module",
  "license": "MIT",
  "keywords": [
    "pi-package",
    "pi-extension",
    "context",
    "grounding"
  ],
  "engines": {
    "node": ">=22.19.0"
  },
  "pi": {
    "extensions": [
      "./extensions/index.ts"
    ]
  },
  "files": [
    "extensions",
    "tests",
    "README.md"
  ],
  "peerDependencies": {
    "@earendil-works/pi-coding-agent": ">=0.84.0 <0.85.0",
    "@earendil-works/pi-tui": ">=0.84.0 <0.85.0"
  },
  "devDependencies": {
    "@earendil-works/pi-coding-agent": "0.84.1",
    "@earendil-works/pi-tui": "0.84.1"
  },
  "scripts": {
    "test": "node --experimental-strip-types --test tests/*.test.ts",
    "syntax": "node --experimental-strip-types --check extensions/index.ts && node --experimental-strip-types --check extensions/snapshot.ts",
    "prepack": "npm run syntax && npm test"
  }
}
