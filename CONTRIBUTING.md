# Contributing

Before adding source, record its authorship, license, public boundary, and provenance.

Keep changes source-only. Do not add dependencies, generated output, binaries, sessions,
credentials, private paths, or deployed state. Run the fixed public-tree check without
network access before review or release.
